 (function() {
        //    let name = 'Sandy';
        //    let score = 6000;

        let powerballScoreList = ['Sandy', 100000, 'Andy', 20000, 'Alex', 10000]

        function showPowerballScoreList() {
            for (i = 0; i < powerballScoreList.length; i++) {
                console.log(powerballScoreList[i]);
            }
        }
        showPowerballScoreList();
    })();